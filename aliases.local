# Nav
alias dotfiles='cd ~/dotfiles-local'
alias dev='cd ~/workspace'
alias work='cd ~/workspace/govly/telescope;'

# Git
alias gits='git status -s'
alias gitc='git cm'
alias gitcm='git c'
alias gita='git add'

# Ruby
alias be='bundle exec'

# Apps
alias github='open https://github.com/weilandia'
alias chrome='open -a "Google Chrome"'
alias spotify='open -a "Spotify"'

# AWS
alias aws='docker pull amazon/aws-cli:latest && docker run --rm -it -v ~/.aws:/root/.aws -v $(pwd):/aws amazon/aws-cli'
